description: Unauthorized. Geçersiz veya eksik Token.
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: 'Not authorized'
